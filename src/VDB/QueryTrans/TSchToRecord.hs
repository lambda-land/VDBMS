-- | turns a table schema to record compatible with records 
--   of tables in haskelldb.
module VDB.QueryTrans.TSchToRecord where

import VDB.Schema 


import qualified Database.HaskellDB as HSDB

-- import Language.Haskell.TH

