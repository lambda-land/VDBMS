-- | Configures a vq to a list of variant queries,
--   configures a vdb to a list of variant dbs,
--   runs each variant query on its correspondent variant db.
--   This is basically the brute force approach that a DBA 
--   has to take with what traditional DBMS has to offer.
-- NOTE: MAYBE DO THIS FOR COMPARISON, BUT AFTER YOU'VE IMPLEMENTED THE REST.
module VDBMS.Approaches.ConfigQ.RunVariantQueryOnVariantDBs where


