-- | generates a relational query with the same schema
--   for each of the relational queries in a list while
--   incorporating the temporary results that have been already stored.
module VDBMS.QueryGen.MySql.GenMulQsSameSchTempResStored where 
